name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  backend:
    name: Backend (Go)
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: nimbus_test
          POSTGRES_USER: nimbus
          POSTGRES_PASSWORD: test_password
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.25'
          cache-dependency-path: backend/go.sum

      - name: Install dependencies
        working-directory: ./backend
        run: |
          go mod download
          go mod verify

      - name: Run go fmt
        working-directory: ./backend
        run: |
          if [ "$(gofmt -s -l . | wc -l)" -gt 0 ]; then
            echo "Go code is not formatted:"
            gofmt -s -l .
            exit 1
          fi

      - name: Run go vet
        working-directory: ./backend
        run: go vet ./...

      - name: Run tests
        working-directory: ./backend
        env:
          DB_HOST: localhost
          DB_PORT: 5432
          DB_NAME: nimbus_test
          DB_USER: nimbus
          DB_PASSWORD: test_password
          JWT_SECRET: test-jwt-secret-key-for-ci-minimum-32-characters-required
          PORT: 8080
          CORS_ORIGINS: http://localhost:3000
        run: go test -v -race -coverprofile=coverage.out -covermode=atomic ./...

      - name: Build
        working-directory: ./backend
        run: go build -v -o bin/nimbus cmd/server/main.go

      - name: Upload coverage
        uses: codecov/codecov-action@v5
        with:
          files: ./backend/coverage.out
          flags: backend
          fail_ci_if_error: false

  frontend:
    name: Frontend (Next.js)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: ./frontend
        run: npm ci

      - name: Check formatting
        working-directory: ./frontend
        run: npm run format-check

      - name: Run linter
        working-directory: ./frontend
        run: npm run lint

      - name: Type check
        working-directory: ./frontend
        run: npx tsc --noEmit

      - name: Run tests
        working-directory: ./frontend
        env:
          JWT_SECRET: test-jwt-secret-key-for-ci-minimum-32-characters-required
        run: npm test

      - name: Build
        working-directory: ./frontend
        env:
          NEXT_PUBLIC_API_URL: http://localhost:8080/api/v1
          JWT_SECRET: test-jwt-secret-key-for-ci-minimum-32-characters-required
        run: npm run build

      - name: Check build output
        working-directory: ./frontend
        run: |
          if [ ! -d ".next" ]; then
            echo "Build failed - .next directory not found"
            exit 1
          fi
          echo "Build successful"

      - name: Upload coverage
        uses: codecov/codecov-action@v5
        with:
          files: ./frontend/coverage/lcov.info
          flags: frontend
          fail_ci_if_error: false

  docker-setup:
    name: Docker Setup
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Test entrypoint script
        run: sh docker/entrypoint.test.sh

  # nimbus.turboot.com prod image
  docker-demo:
    name: Build Demo Image
    runs-on: ubuntu-latest
    needs: [backend, frontend, docker-setup]
    if: |
      github.ref == 'refs/heads/main' &&
      github.event_name == 'push' &&
      github.repository_owner == 'Turbootzz' &&
      needs.backend.result == 'success' &&
      needs.frontend.result == 'success' &&
      needs.docker-setup.result == 'success'
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push production frontend
        uses: docker/build-push-action@v6
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: true
          tags: ghcr.io/turbootzz/nimbus-frontend-prod:latest
          platforms: linux/amd64,linux/arm64
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            NEXT_PUBLIC_APP_NAME=Nimbus
            NEXT_PUBLIC_SITE_URL=${{ secrets.NEXT_PUBLIC_SITE_URL }}

  # Nimbus Cloud image
  docker-cloud:
    name: Build Cloud Image
    runs-on: ubuntu-latest
    needs: [backend, frontend, docker-setup]
    if: |
      github.ref == 'refs/heads/main' &&
      github.event_name == 'push' &&
      github.repository_owner == 'Turbootzz' &&
      needs.backend.result == 'success' &&
      needs.frontend.result == 'success' &&
      needs.docker-setup.result == 'success'
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push cloud image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ghcr.io/turbootzz/nimbus-cloud:latest
            ghcr.io/turbootzz/nimbus-cloud:${{ github.sha }}
          platforms: linux/amd64,linux/arm64
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            NEXT_PUBLIC_APP_NAME=Nimbus
            NEXT_PUBLIC_NIMBUS_CLOUD=true

  # Public Docker Hub images
  docker:
    name: Build and Push Docker Images
    runs-on: ubuntu-latest
    needs: [backend, frontend, docker-setup]
    if: |
      github.ref == 'refs/heads/main' &&
      github.event_name == 'push' &&
      github.repository_owner == 'Turbootzz' &&
      needs.backend.result == 'success' &&
      needs.frontend.result == 'success' &&
      needs.docker-setup.result == 'success'

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # Custom PostgreSQL image with auto-migration
      - name: Extract metadata for postgres
        id: meta-postgres
        uses: docker/metadata-action@v5
        with:
          images: ${{ secrets.DOCKERHUB_USERNAME }}/nimbus-postgres
          tags: |
            type=raw,value=18
            type=raw,value=latest

      - name: Build and push postgres image
        uses: docker/build-push-action@v6
        with:
          context: ./docker/postgres
          file: ./docker/postgres/Dockerfile
          push: true
          tags: ${{ steps.meta-postgres.outputs.tags }}
          labels: ${{ steps.meta-postgres.outputs.labels }}
          platforms: linux/amd64,linux/arm64
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # Unified all-in-one image (recommended for most users)
      - name: Extract metadata for unified image
        id: meta-unified
        uses: docker/metadata-action@v5
        with:
          images: ${{ secrets.DOCKERHUB_USERNAME }}/nimbus
          tags: |
            type=raw,value=latest
            type=sha,prefix={{branch}}-

      - name: Build and push unified image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ steps.meta-unified.outputs.tags }}
          labels: ${{ steps.meta-unified.outputs.labels }}
          platforms: linux/amd64,linux/arm64
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            NEXT_PUBLIC_APP_NAME=Nimbus

      # Separate images (for advanced users using docker-compose.deprecated.yml)
      - name: Extract metadata for backend
        id: meta-backend
        uses: docker/metadata-action@v5
        with:
          images: ${{ secrets.DOCKERHUB_USERNAME }}/nimbus-backend
          tags: |
            type=raw,value=latest
            type=sha,prefix={{branch}}-

      - name: Extract metadata for frontend
        id: meta-frontend
        uses: docker/metadata-action@v5
        with:
          images: ${{ secrets.DOCKERHUB_USERNAME }}/nimbus-frontend
          tags: |
            type=raw,value=latest
            type=sha,prefix={{branch}}-

      - name: Build and push backend
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: ${{ steps.meta-backend.outputs.tags }}
          labels: ${{ steps.meta-backend.outputs.labels }}
          platforms: linux/amd64,linux/arm64
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push frontend
        uses: docker/build-push-action@v6
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: true
          tags: ${{ steps.meta-frontend.outputs.tags }}
          labels: ${{ steps.meta-frontend.outputs.labels }}
          platforms: linux/amd64,linux/arm64
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            NEXT_PUBLIC_APP_NAME=Nimbus

  ci-success:
    name: CI Success
    runs-on: ubuntu-latest
    needs: [backend, frontend, docker-setup, docker, docker-demo, docker-cloud]
    if: always()
    steps:
      - name: Check all jobs
        run: |
          # Docker build jobs are skipped on non-main branches, so treat 'skipped' as success
          docker_ok=$([[ "${{ needs.docker.result }}" == "success" ]] || [[ "${{ needs.docker.result }}" == "skipped" ]] && echo "true" || echo "false")
          demo_ok=$([[ "${{ needs.docker-demo.result }}" == "success" ]] || [[ "${{ needs.docker-demo.result }}" == "skipped" ]] && echo "true" || echo "false")
          cloud_ok=$([[ "${{ needs.docker-cloud.result }}" == "success" ]] || [[ "${{ needs.docker-cloud.result }}" == "skipped" ]] && echo "true" || echo "false")

          if [[ "${{ needs.backend.result }}" == "success" ]] && [[ "${{ needs.frontend.result }}" == "success" ]] && [[ "${{ needs.docker-setup.result }}" == "success" ]] && [[ "$docker_ok" == "true" ]] && [[ "$prod_ok" == "true" ]] && [[ "$cloud_ok" == "true" ]]; then
            echo "All CI jobs passed!"
            exit 0
          else
            echo "Some CI jobs failed"
            echo "Backend: ${{ needs.backend.result }}"
            echo "Frontend: ${{ needs.frontend.result }}"
            echo "Docker Setup: ${{ needs.docker-setup.result }}"
            echo "Docker: ${{ needs.docker.result }}"
            echo "Docker Prod: ${{ needs.docker-demo.result }}"
            echo "Docker Cloud: ${{ needs.docker-cloud.result }}"
            exit 1
          fi
